load("//:test_rules.bzl", "itest_go_test")

itest_go_test(
    name = "args_test",
    srcs = ["args_test.go"],
    args = [
        "--test-arg=hello",
        "--test-port=$${@@//:autoassigned}",
    ],
    services = ["//:autoassigned"],
)
