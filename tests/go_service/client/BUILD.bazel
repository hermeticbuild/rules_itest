load("//:test_rules.bzl", "itest_go_test")

itest_go_test(
    name = "client_test",
    srcs = ["client_test.go"],
    env = {
        "TEST_PORT": "$${@@//:autoassigned}",
    },
    services = ["//:autoassigned"],
)
